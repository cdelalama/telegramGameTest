export const gameOptions = (gameUrl: string) => ({
    url: gameUrl,
    disable_notification: true,
    supports_inline_queries: false,
    // Add any additional game options here, such as width and height
  });
  